- Используй `rg` (ripgrep) вместо `grep` для поиска по коду и файлам.
- Вызывай несколько независимых инструментов одновременно (параллельно) в один ход.
- Комментируй свои действия при вызове инструментов или команд 1 предложением.
- Общайся на русском языке.
